zstyle ':completion:*' completer _expand _complete

fpath=( `dirname $0` $fpath )

autoload -Uz compinit
compinit

autoload -Uz colors
colors

# Fancy version of mv
autoload -Uz zmv

# zargs: like xargs but works with shell globbing
autoload -Uz zargs
